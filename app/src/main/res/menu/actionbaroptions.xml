<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Location settings, should never be in the overflow -->
    <item android:id="@+id/action_location_settings"
        android:icon="@drawable/trackpointcustom"
        android:title="@string/actionBarLocationSettings"
        app:showAsAction="always"/>

    <!-- "settings", should appear as action button if possible -->
    <item android:id="@+id/action_settings"
        android:icon="@drawable/settingscustom"
        android:title="@string/actionBarSettings"
        app:showAsAction="always"/>

</menu>